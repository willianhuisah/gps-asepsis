const mongoose = require('mongoose'); mongoose.connect('mongodb://127.0.0.1:27017/asepsis_gps').then(async () => { const Ayudante = mongoose.model('Ayudante', new mongoose.Schema({ nombre: String })); const nombres = ['Miguel Angel', 'Roberto Carlos', 'Daniel Torres', 'Fernando Silva', 'Julio Cesar', 'Andrés Mendoza', 'David Castro', 'Gabriel León']; for (let n of nombres) { const existe = await Ayudante.findOne({nombre: n}); if (!existe) { await new Ayudante({nombre: n}).save(); } } console.log('Ayudantes inyectados en MongoDB.'); process.exit(0); });
