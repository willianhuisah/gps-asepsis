const mongoose=require('mongoose'); mongoose.connect('mongodb://127.0.0.1:27017/asepsis_gps').then(async()=>{ const Vehiculo=mongoose.model('Vehiculo',new mongoose.Schema({}, {strict:false})); const vs=await Vehiculo.find(); for(let v of vs){ v.ruta=[]; v.puntoActual=0; v.ayudante=''; v.horaSalida=''; v.horaRegreso=''; await v.save(); } console.log('Limpiado'); process.exit(0); });
